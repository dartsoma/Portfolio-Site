import "./assets/SideNav.css";
import bracket from "./assets/bracket.png";
import placeholder from "./assets/placeholdericon.png";

function SideNav({ globalPage, setPageState }) {
  const togglePage = (index) => {
    setPageState(index);
  };
  return <></>;
}

export default SideNav;
